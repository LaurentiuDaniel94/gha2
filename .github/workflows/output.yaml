name: output info
on: workflow_dispatch
jobs:
    info:
        runs-on: ubuntu-latest
        steps:
            - name: github context
              run: echo "${{ toJson(github.repository_owner)}}"